<Window x:Class="Cypres2._0.Views.ManoDeObra.FamiliaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cypres2._0.Views.ManoDeObra"
        
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="es"
        lex:ResxLocalizationProvider.DefaultAssembly="Cypres2.0"
        lex:ResxLocalizationProvider.DefaultDictionary="Properties.Resources.ManoDeObra.Strings"
        
        Title="{Binding Title}"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        mc:Ignorable="d"
        Height="150" Width="450">


    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Descripcion field -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                   Text="{lex:Loc GridDescripcion}"
                   FontFamily="{StaticResource SecondaryFont}"
                   FontSize="{StaticResource FontSizeNormal}"
                   Margin="0,0,10,0"
                   VerticalAlignment="Center"/>
            <TextBox Grid.Column="1"
                 Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}"
                 FontFamily="{StaticResource SecondaryFont}"
                 FontSize="{StaticResource FontSizeNormal}"
                 Height="25"
                 VerticalAlignment="Center"
                     VerticalContentAlignment="Center"/>
        </Grid>

        <!-- Buttons -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1"
            Content="{lex:Loc BotonAceptar}"
            Command="{Binding AceptarCommand}"
            Margin="0,0,10,0"
            Padding="15,5"/>
            <Button Grid.Column="2"
            Content="{lex:Loc BotonTerminar}"
            Command="{Binding TerminarCommand}"
            Padding="15,5"/>
        </Grid>
    </Grid>



</Window>
