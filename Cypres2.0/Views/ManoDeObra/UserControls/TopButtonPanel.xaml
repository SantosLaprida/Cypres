<UserControl x:Class="Cypres2._0.Views.ManoDeObra.UserControls.TopButtonPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cypres2._0.Views.ManoDeObra.UserControls"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             lex:LocalizeDictionary.DesignCulture="es"
             lex:ResxLocalizationProvider.DefaultAssembly="Cypres2.0"
             lex:ResxLocalizationProvider.DefaultDictionary="Properties.Resources.ManoDeObra.Strings"
             
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Cypres2.0;component/Views/ManoDeObra/UserControls/Icons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource MenuBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>


        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <!-- Icon Button - Column 0 -->
            <Button Grid.Column="0"
                Style="{StaticResource IconButtonStyle}"
                ToolTip="{lex:Loc BotonCrear}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <Image Source="{StaticResource FileIcon}" Width="22" Height="22"/>
            </Button>
            <Button Grid.Column="1"
                Style="{StaticResource IconButtonStyle}"
                ToolTip="{lex:Loc BotonEditar}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <Image Source="{StaticResource EditFileIcon}" Width="22" Height="22"/>
            </Button>
            <Button Grid.Column="2"
                Style="{StaticResource IconButtonStyle}"
                ToolTip="{lex:Loc BotonEliminar}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <Image Source="{StaticResource FileRemoveIcon}" Width="22" Height="22"/>
            </Button>
            <Rectangle Grid.Column="3" Width="1" Fill="#CCCCCC" VerticalAlignment="Stretch"/>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                Style="{StaticResource IconButtonStyle}"
                ToolTip="{lex:Loc BotonMayuscula}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <Image Source="{StaticResource UpperCase}" Width="22" Height="22"/>
            </Button>

            <Button Grid.Column="1"
                Style="{StaticResource IconButtonStyle}"
                ToolTip="{lex:Loc BotonMinuscula}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <Image Source="{StaticResource LowerCase}" Width="22" Height="22"/>
            </Button>

        </Grid>
    </Grid>
</UserControl>
